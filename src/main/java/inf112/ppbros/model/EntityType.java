package inf112.ppbros.model;

/** Entities can be of different types, the different types are listed in EntityType */
public enum EntityType {
    MAIN_CHARACTER,
    ENEMY, // change name if there are different types of eneimies
}
